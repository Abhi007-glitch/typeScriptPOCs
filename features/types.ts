const today = new Date();
today.getFullYear(); // today. -> will suggest us all the methods linked to that type 

const person ={ // on hovering over person variable instead of showing the name
                // of the type it shows whole object structure.
  age:20
}


class Color {
   
  public method1()
  {

  }
}


const read =new Color(); // read is of type named as color
read.method1();